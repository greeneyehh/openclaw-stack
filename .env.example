# OpenClaw Docker Stack – Umgebungsvariablen
# Kopiere diese Datei nach .env und passe Werte an:
#   cp .env.example .env

# Verzeichnisse auf dem Host (werden als Volumes gemountet)
OPENCLAW_CONFIG_DIR=${HOME}/.openclaw
OPENCLAW_WORKSPACE_DIR=${HOME}/.openclaw/workspace

# Ports
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790

# Bind-Adresse: loopback (nur localhost), lan (Netzwerk), oder 0.0.0.0
OPENCLAW_GATEWAY_BIND=lan

# Token für Control UI / API (wird von docker-setup.sh erzeugt, wenn leer)
OPENCLAW_GATEWAY_TOKEN=

# Docker-Image (Standard: lokal gebaut)
OPENCLAW_IMAGE=openclaw:local

# Optionale OAuth/API-Keys (in openclaw.json oder hier)
# CLAUDE_AI_SESSION_KEY=
# CLAUDE_WEB_SESSION_KEY=
# CLAUDE_WEB_COOKIE=

# Optionale Zusatz-Mounts (kommagetrennt), z. B.:
# OPENCLAW_EXTRA_MOUNTS=$HOME/.codex:/home/node/.codex:ro
# OPENCLAW_EXTRA_MOUNTS=

# Optional: Named Volume für /home/node (persistiert Browser-Cache etc.)
# OPENCLAW_HOME_VOLUME=openclaw_home
# OPENCLAW_HOME_VOLUME=

# Optional: Zusätzliche apt-Pakete beim Image-Build (leer lassen wenn nicht nötig)
# OPENCLAW_DOCKER_APT_PACKAGES=ffmpeg git curl jq
# OPENCLAW_DOCKER_APT_PACKAGES=

# Optional: WireGuard (nur bei Nutzung von docker-compose.vpn.yml)
# Pfad zur wg0.conf (Standard: ./wireguard/wg0.conf)
# OPENCLAW_WIREGUARD_CONF=./wireguard/wg0.conf
